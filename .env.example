"""
This file is a template for the .env file.

Please copy this file to .env and fill in the values.

For more information about configuration options, please refer to the documentation

"""

# Global configs:
USE_AZURE=False
CHAT_USE_AZURE_TOKEN_PROVIDER=False
EMBEDDING_USE_AZURE_TOKEN_PROVIDER=False
MAX_RETRY=5
RETRY_WAIT_SECONDS=5
FACTOR_MINING_TIMEOUT=10800 # 最大运行时间
USE_LOCAL=True

# LLM API Setting:
OPENAI_BASE_URL=<your_base_url>
OPENAI_API_KEY=<your_api_key>
CHAT_MODEL=<your_chat_model> # e.g., "deepseek-v3"
REASONING_MODEL=<your_reasoning_model> # e.g. "deepseek-reasoner"
EMBEDDING_MODEL=text-embedding-3-small # RAG

CHAT_MAX_TOKENS=4000
CHAT_TEMPERATURE=0.7

# LLM logs: prompts/responses under log/*/llm_messages/ (viewable in alphapilot ui)
LOG_LLM_CHAT_TO_FILE=True
LOG_LLM_CHAT_TO_CONSOLE=False
# Legacy (optional): LOG_LLM_CHAT_CONTENT=True enables both file and console

# Data paths (optional overrides; defaults resolve under ~/.qlib/qlib_data/cn_data/)
# Baostock CSV:   cn_data/baostock/raw_data_{no,forward,back}_adjust
# Baostock Qlib:  cn_data/baostock/qlib
# Tushare CSV:    cn_data/tushare/raw_data_{no,forward,back}_adjust
# Tushare Qlib:   cn_data/tushare/qlib
# ALPHAPILOT_QLIB_DATA_DIR=~/.qlib/qlib_data/cn_data/baostock/qlib
# ALPHAPILOT_RAW_DATA_DIR=~/.qlib/qlib_data/cn_data/baostock/raw_data_back_adjust
# ALPHAPILOT_ADJUST_FACTOR_DIR=~/.qlib/qlib_data/cn_data/baostock/adjust_factors
# TUSHARE_TOKEN=<your_tushare_pro_token>

# Qlib factor backtest / mine / strategy_backtest (optional; shared QLIB_FACTOR_ prefix)
# Template folder copied into each RD-Agent_workspace before qrun (.py/.yaml/.md only)
# QLIB_FACTOR_QLIB_TEMPLATE_DIR=important_data/factor_qlib_templates
# ALPHAPILOT_STRATEGY_PARAM_DIR=important_data/strategy_zoo
# ALPHAPILOT_FACTOR_ZOO_DIR=important_data/factor_zoo
# ALPHAPILOT_IMPORTANT_DATA_DIR=important_data
# QLIB_FACTOR_QLIB_CONFIG_NAME=conf_cn_combined_kdd_ver.yaml

# Pickle cache (mine vs backtest use separate roots by default)
# ALPHAPILOT_PICKLE_CACHE_ENABLED=true
# ALPHAPILOT_PICKLE_CACHE_DIR_MINE=pickle_cache/mine
# ALPHAPILOT_PICKLE_CACHE_DIR_BACKTEST=pickle_cache/backtest
# Legacy single root when scope is unset (also: PICKLE_CACHE_FOLDER_PATH_STR)
# ALPHAPILOT_PICKLE_CACHE_DIR=pickle_cache

# Factor factor.py subprocess (default: current interpreter sys.executable)
# FACTOR_CoSTEER_PYTHON_BIN=/path/to/conda/envs/alphapilot/bin/python

# Live trading / broker gateway settings.
# Keep real credentials in your private .env only; do not commit them.
# Automated strategy routing is fail-closed unless explicitly enabled.
ALPHAPILOT_AUTOMATED_LIVE_ENABLED=false
# Higher-priority compatibility override for the CLI-managed Portal auth mode.
# Default/saved mode is "required". Prefer:
#   alphapilot portal_operator_auth --required=false --operator_id=alice \
#     --reason="trusted lab network" --acknowledge_network_risk=true
# WARNING: false allows every reachable client and cross-origin page to call all
# /api/live and /api/trading writes without a token, including LIVE orders.
# ALPHAPILOT_OPERATOR_AUTH_REQUIRED=true
# Stable, non-secret deployment identifier used by compatibility call evidence.
# ALPHAPILOT_ENVIRONMENT_ID=paper-host-a
# ALPHAPILOT_STRATEGY_DIR=strategies
# ALPHAPILOT_PORTFOLIO_POLICY_DIR=policies
# ALPHAPILOT_TRADING_BACKTEST_WORKERS=2
# ALPHAPILOT_LIVE_BUY_FEE_RATE=0.00015
# ALPHAPILOT_LIVE_SELL_FEE_RATE=0.00015
# ALPHAPILOT_LIVE_MIN_FEE=5.0
# ALPHAPILOT_LIVE_MODE=live
# ALPHAPILOT_LIVE_BROKER=xtp
# ALPHAPILOT_LIVE_XTP_ACCOUNT=<your_xtp_account>
# ALPHAPILOT_LIVE_XTP_PASSWORD=<your_xtp_password>
# ALPHAPILOT_LIVE_XTP_CLIENT_ID=1
# ALPHAPILOT_LIVE_XTP_SOFTWARE_KEY=<your_xtp_software_key>
# ALPHAPILOT_LIVE_XTP_QUOTE_HOST=<quote_host_from_xtp_email>
# ALPHAPILOT_LIVE_XTP_QUOTE_PORT=<quote_port_from_xtp_email>
# ALPHAPILOT_LIVE_XTP_TRADE_HOST=<trade_host_from_xtp_email>
# ALPHAPILOT_LIVE_XTP_TRADE_PORT=<trade_port_from_xtp_email>
# ALPHAPILOT_LIVE_XTP_QUOTE_PROTOCOL=TCP
# ALPHAPILOT_LIVE_XTP_LOG_LEVEL=INFO

# Bounded real-broker UAT. Keep disabled except during an attended local CLI run.
ALPHAPILOT_BROKER_UAT_ENABLED=false
# ALPHAPILOT_BROKER_UAT_ENVIRONMENT=protected-test-account-a
# ALPHAPILOT_BROKER_UAT_WHITELIST=600000.SSE
# ALPHAPILOT_BROKER_UAT_MAX_NOTIONAL=20000
# Optional descriptive metadata only. UAT trust is bound to native SDK, plugin,
# gateway and live-runtime SHA-256 fingerprints computed by the harness.
# ALPHAPILOT_XTP_SDK_VERSION=<descriptive_xtp_sdk_version>
# ALPHAPILOT_EMT_SDK_VERSION=<descriptive_emt_sdk_version>
